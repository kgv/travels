[{{ site.data.strings[site.locale][include.type] }}: {% if include.name %}{{ include.name }}{% else %}{{ include.coordinates.latitude }}, {{ include.coordinates.longitude }}{% endif %}]: {{ include.links[0] }}