[{{ site.data.strings[site.locale][include.toponym.type] | capitalize }}: {% if include.toponym.name %}{{ include.toponym.name }}{% else %}{{ include.toponym.coordinates.latitude }}, {{ include.toponym.coordinates.longitude }}{% endif %}]: {{ include.toponym.links[0] }}