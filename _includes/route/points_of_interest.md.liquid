## {{ site.data.strings[site.locale].points_of_interest | capitalize }}

{% for tag in page.tags %}
{% assign parts = tag | split: " -- " %}
- [{{ parts.first }} {% if parts[2] %}"{{ parts[1] }}"{% else %}{{ parts[1] }}{% endif %}][{{ tag }}]{: target="_blank" title="{{ parts.last }}" }
{% endfor %}